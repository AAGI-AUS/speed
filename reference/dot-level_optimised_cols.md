# Columns a Single Level Optimises or Scores On

Everything one level of `optimise` needs present in the design: the
column it swaps, plus the fixed layout columns it groups and scores by.

## Usage

``` r
.level_optimised_cols(opt)
```

## Arguments

- opt:

  One level of the `optimise` list.

## Value

A character vector of column names.
